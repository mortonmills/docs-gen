{
  "name": "docs-gen",
  "version": "1.0.0",
  "description": "The docs-gen repo is a document generator which uses nodejs to arrange files and pandoc to generate them.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "pandoc-filter": "^2.2.0",
    "readability-cli": "^2.4.5"
  }
}
